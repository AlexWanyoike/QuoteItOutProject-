import { GithubapipipePipe } from './githubapipipe.pipe';

describe('GithubapipipePipe', () => {
  it('create an instance', () => {
    const pipe = new GithubapipipePipe();
    expect(pipe).toBeTruthy();
  });
});
